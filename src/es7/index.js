let numero = 2;
let numbers = [1,2,3,4,5,7,5,6,9];

if (numbers.includes(numero)) {
    console.log(`si se encuentra el valor ${numero}`);
} else {
    console.log(`no se encuentra el valor ${numero}`);
}

let base = 2, exponent = 8
let result = base ** exponent;
console.log(result);